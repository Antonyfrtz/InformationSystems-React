export type UserProfileToken = {
    userName: string;
    email: string;
    token: string;
    roles: string[];
}

export type UserProfile = {
    userName: string;
    email: string;
    role: string;
}